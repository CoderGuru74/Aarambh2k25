/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['your-domain.com'], // add image domains if using <Image> component
    },
  };
  
  module.exports = nextConfig;
  